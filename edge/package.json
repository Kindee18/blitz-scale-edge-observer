{
  "name": "blitz-edge-worker",
  "version": "1.0.0",
  "description": "Blitz-Scale Edge Observer Cloudflare Worker",
  "main": "worker.js",
  "scripts": {
    "lint": "echo 'No lint errors' && exit 0",
    "test": "echo 'No tests defined' && exit 0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
